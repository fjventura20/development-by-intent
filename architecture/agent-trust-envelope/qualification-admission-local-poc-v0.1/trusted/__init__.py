"""ATE Qualification & Admission Local PoC v0.1 — trusted package marker.

The `trusted/` directory contains executor-owned code. It is installed
by bootstrap into a root-owned, non-requester-writable location.
"""
