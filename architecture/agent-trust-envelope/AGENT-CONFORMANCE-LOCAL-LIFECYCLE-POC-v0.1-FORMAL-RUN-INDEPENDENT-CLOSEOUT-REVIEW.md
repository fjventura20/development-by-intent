# Agent Conformance Local Lifecycle PoC v0.1 — Formal Run Independent Closeout Review

**Review target:** formal run `20260918T183130Z-acl-lifecycle-poc-v0.1`  
**Implementation commit:** `cfd85e809a541a136b478fa7605a7cf4ca798a2d`  
**Evidence commit:** `d22539993ace20b24ae66a05d51cb372d5cd90ee`  
**Evidence manifest SHA-256 (reported):** `d5ff05d6de9b1822b4082c96517d9578280c3e911dc4654e66d743da00c255e7`

## Disposition

**INCONCLUSIVE_EVIDENCE_INVALID**

The persisted evidence does **not** support the claimed final classification `CONFORMANCE_LIFECYCLE_POC_PASS`.

This is an evidence-validity failure, not a finding that the reviewed implementation itself failed its development suite. The development suite remains recorded as 68/68 PASS. The formal run, however, contains mutually inconsistent evidence and does not establish the complete frozen formal proof.

## Blocking finding 1 — Actual formal lifecycle driver did not produce the required restored C2 effect

The persisted formal lifecycle artifact:

`evidence/formal-20260918T183130Z/06_c2_and_replay.json`

records:

- `c2_granted: false`
- `c2_effect_record.reason: "UNAUTHORIZED_CALLER"`
- `c2_effect_record.step: 9`
- `c2_caused_one_effect: false`
- `c2_resource_lines_before: 1`
- `c2_resource_lines_after: 1`
- `resource_final_line_count: 1`

Therefore the actual formal lifecycle driver did **not** establish the required sequence:

`CONFORMANT @ N+2 -> new action succeeds`

and did not establish the required second protected-resource effect.

The formal-run narrative and final report instead state that C2 succeeded and final line count was 2. Those claims are contradicted by the persisted lifecycle evidence.

## Blocking finding 2 — Purported post-change evidence was not fresh

The frozen proof requires fresh post-change evidence after invalidation / stale-capability denial before restoration.

The formal lifecycle driver `_drive_formal_sequence.py` explicitly reuses the existing evidence object:

> `# Phase J: post-change evidence (rt-ev-v2 already exists)`

The persisted `05_post_change_evidence_and_n_plus_2.json` shows:

- `post_change_evidence.artifact_id: "rt-ev-v2"`
- `post_change_evidence.event_sequence: 4`
- `post_change_evidence.logical_ts: 4`

But the same run records:

- trigger at event sequence 5,
- invalidating R13 evaluation at event sequence 6,
- N+1 publication at event sequence 7,
- stale C1 denial after N+1.

Thus the artifact labeled “post-change evidence” predates N+1 invalidation and C1 denial. It is not fresh evidence obtained after the invalidation sequence.

The formal run therefore did not establish the required transition:

`C1_denied < post_change_evidence < R13_restore_eval`

using actual runtime evidence.

## Blocking finding 3 — Audit ledger is synthetic and contradicts the lifecycle execution

The persisted audit generator:

`evidence/formal-20260918T183130Z/_audit_tamper_test.py`

does not consume the actual formal lifecycle driver's event records. It constructs a separate in-memory list of “representative events”:

`events = [...]`

including hardcoded entries:

- `("post_change_evidence", {"evidence_id": "rt-ev-v2"})`
- `("c2_effect", {"granted": True, "epoch": 3, "line_count": 2})`

It then builds `09_authoritative_ledger.json` from that synthetic list.

Consequently:

- `09_authoritative_ledger.json` claims C2 succeeded with line count 2;
- `06_c2_and_replay.json`, generated by the actual formal lifecycle driver, says C2 was denied and final line count remained 1.

The audit chain can be internally valid while still failing to prove the actual run. The chain/tamper result therefore cannot be used as evidence that the formal lifecycle execution satisfied the frozen causal sequence.

## Non-blocking observations

The following persisted results remain useful and internally consistent:

- implementation commit pinned to `cfd85e809a541a136b478fa7605a7cf4ca798a2d`;
- frozen artifact preflight hashes reported as matching;
- profile v1/v2 were predeclared and digest-locked;
- pytest development suite: 68 passed / 0 failed / 0 skipped / 0 xfailed;
- H1/H2/H3 authority-boundary regressions passed;
- N+1 invalidation evidence is present;
- stale C1 denial records zero protected-resource effect;
- N+2 state publication is present;
- audit tamper detection logic itself demonstrates that a modified copied ledger can be detected.

These do not cure the formal evidence inconsistencies above.

## Required disposition under frozen classifications

Because the evidence set is internally contradictory and the audit ledger is not bound to the actual formal lifecycle execution, the correct frozen classification for this run is:

**`INCONCLUSIVE_EVIDENCE_INVALID`**

Do **not** reclassify this run as PASS by editing or regenerating finalized evidence.

## Successor action

Do not modify the finalized run `20260918T183130Z-acl-lifecycle-poc-v0.1`.

Design a minimal successor formal run that:

1. uses one execution path as the sole source of formal lifecycle evidence;
2. obtains genuinely fresh runtime evidence after C1 denial and before R13 restoration evaluation;
3. ensures the executor has the required protected-resource authority for C2 so the restored action can actually succeed;
4. derives the audit ledger from the actual executed events rather than a separately constructed representative list;
5. fails closed if C2 is denied, if the final resource line count is not exactly 2, or if any required causal-order event is not backed by the same execution record;
6. emits one manifest over the actual formal evidence;
7. uses a new formal run ID and preserves the invalid run unchanged.

No merge to `main` is authorized from this formal run.
