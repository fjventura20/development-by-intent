# COA-E2 v0.4 package

Targeted one-pair proof of concept. This directory is draft-only and not frozen. The only controlling packet is `packets/CANONICAL-INITIALIZATION-TEMPLATE-DRAFT.md`; the only participant prompts are `tasks/participant/U2.md`, `U3.md`, and `U5.md`. Outcome mappings exist only in `tasks/RUBRIC-DRAFT.md`.

The controlling deviation document is `DEVIATION-AND-STOP-RULES-DRAFT.md`.

Two isolated profiles, two sessions, six turns per session, 12 experimental turns total. No final nonces, profile creation, mechanism test, participant invocation, scoring, evaluator, or freeze is authorized.
