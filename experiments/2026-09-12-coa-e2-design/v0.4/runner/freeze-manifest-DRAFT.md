# COA-E2 v0.4 Freeze Manifest — DRAFT

At a future freeze, this manifest will list every controlling v0.4 file relative to the declared package root, excluding the manifest itself. The external freeze record will contain this JSON's SHA-256. It will contain exactly two final nonces in a separate runtime manifest. No v0.3 file is controlling. Freeze is blocked until the mechanism check, dry-run, prompt/rubric separation, packet token tolerance, two-profile freshness/equivalence, and consistency audit pass.
