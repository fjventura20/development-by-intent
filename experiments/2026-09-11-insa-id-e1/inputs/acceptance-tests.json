{
  "$schema": "INSA-v0.3-acceptance-tests-v1",
  "schema_version": "1.0",
  "record_kind": "acceptance-tests-A",
  "purpose": "Frozen A per proposal v4 §5.1 INV-A-1: per-dimension acceptance tests bound only to M, with evaluator-blinding manifest.",
  "experiment_id": "INSA-ID-E1",
  "binding": {
    "proposal_sha": "ed95705632027f459b396cd423eae54e8bb9a81b",
    "M_source": "inputs/mutation-dimensions.json"
  },
  "tests": [
    {
      "id": "A-mod-a",
      "evaluator": "A (Codex CLI invoking gpt-5.6-sol, blinded)",
      "M_target_id": "M-1",
      "test_kind": "structural + content",
      "rubric": [
        "T1: exactly one near-date event present in the output (count == 1)",
        "T2: the event date is within +/-30 calendar days of the birth date",
        "T3: the event is historically significant (operator-judged pre-piloted rubric; matches v0.1 evaluator packet template §B)",
        "T4: the event is clearly distinguished from exact-date connections in the output text (explicit label or framing distinction)"
      ],
      "pass_rule": "All four T1..T4 must PASS for the test to PASS",
      "per_evaluator_independence_required": true
    },
    {
      "id": "A-mod-b",
      "evaluator": "B (Claude Code CLI invoking claude-opus-4-7, fresh session per I-AUTH-05, blinded)",
      "M_target_id": "M-1",
      "test_kind": "structural + content",
      "rubric": [
        "T1: exactly one near-date event present in the output (count == 1)",
        "T2: the event date is within +/-30 calendar days of the birth date",
        "T3: the event is historically significant (operator-judged pre-piloted rubric; matches v0.1 evaluator packet template §B)",
        "T4: the event is clearly distinguished from exact-date connections in the output text (explicit label or framing distinction)"
      ],
      "pass_rule": "All four T1..T4 must PASS for the test to PASS",
      "per_evaluator_independence_required": true
    },
    {
      "id": "A-mod-c",
      "evaluator": "A re-scored by A (intra-evaluator stability check; report only, not gating)",
      "M_target_id": "M-1",
      "test_kind": "intra-evaluator reliability",
      "rubric": ["same as A-mod-a"],
      "pass_rule": "Report-only; not part of Modification_Success aggregation. Captured for evidence profile.",
      "per_evaluator_independence_required": false
    },
    {
      "id": "A-mod-d",
      "evaluator": "B re-scored by B (intra-evaluator stability check; report only, not gating)",
      "M_target_id": "M-1",
      "test_kind": "intra-evaluator reliability",
      "rubric": ["same as A-mod-b"],
      "pass_rule": "Report-only; not part of Modification_Success aggregation. Captured for evidence profile.",
      "per_evaluator_independence_required": false
    }
  ],
  "aggregation_rule_for_Modification_Success": "Modification_Success = A-mod-a AND A-mod-b (per proposal v4 §5.4 STEP 1). A-mod-c and A-mod-d are report-only.",
  "evaluator_blinding_manifest": {
    "blinded_artifacts": [
      "Evaluator A and B receive only the candidate output + evaluator input packet (no arm identity, no reconstruction number, no experiment identity).",
      "Evaluator blinding packet: evaluation/evaluator-input-packet.md (frozen at protocol freeze)."
    ],
    "de_blinding_table": "results/de-blinding-table.json (auditable; constructed post-scoring from runs/<R>/<B>/<arm>/<candidate> mapping)"
  },
  "frozen_at_utc": "2026-09-11T00:00:00Z",
  "modification_permitted_after_freeze": false
}